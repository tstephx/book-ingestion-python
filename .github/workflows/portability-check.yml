name: Portability check

on:
  push:
  pull_request:

jobs:
  check-hardcoded-paths:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check tracked Claude config for hardcoded /Users/ paths
        run: bash scripts/check_portability.sh
